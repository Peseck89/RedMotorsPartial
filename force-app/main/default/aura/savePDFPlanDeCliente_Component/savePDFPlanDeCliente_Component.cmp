<aura:component implements="flexipage:availableForAllPageTypes,force:hasRecordId,lightning:actionOverride,lightning:isUrlAddressable,force:lightningQuickAction,lightning:availableForFlowScreens" access="global" controller="savePDFfile">
    
    <aura:attribute name="messagePDF" type="String"/>
    <aura:attribute name="instanceURL" type="String"/>
    <aura:attribute name="descuentoAprobado" type="Boolean" default="false"/>
    <aura:attribute name="permisosVisualizacion" type="Boolean" default="false"/>
    <aura:handler name="init" action="{!c.doInit}" value="{!this}"/>

    <!--aura handler with waiting and donewaiting events-->
    <aura:handler event="aura:waiting" action="{!c.showSpinner}"/>
    <aura:handler event="aura:doneWaiting" action="{!c.hideSpinner}"/>    
    
    <!--loading spinner start-->
    <div class="exampleHolder">
        <lightning:spinner aura:id="mySpinner" class="slds-hide"/>
    </div>
    <!-- Loading spinner end-->  

    <aura:if isTrue="{! v.messagePDF == 'Success'}">
        <!-- <iframe src="{! v.instanceURL + '/apex/cT_newPlanCliente?Id=' + v.recordId }" width="100%" height="1000px;" frameBorder="0"/> -->
        <span>Se abrio el pdf en una ventana nueva.</span>
    </aura:if>

    <!-- Funcionamiento que se desactivo para la restriccion de la descarga y visualizacion dependiendo del descuento aprovado 20/08/23-->
    <!--aura:if isTrue="{! !v.descuentoAprobado}">
        <div class="slds-form__item" role="listitem">
            <div class="slds-form-element slds-form-element_horizontal ">  
                <span>Para visualizar el Plan de Cliente el descuento pendiente debe ser aprobado por quien corresponda. </span>
            </div>
        </div>
    </aura:if-->
    
</aura:component>